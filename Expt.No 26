#include <stdio.h>

int main()
{
    FILE *fp;
    char str[100];

    fp = fopen("file.txt","w");

    fprintf(fp,"Operating Systems Lab");
    fclose(fp);

    fp = fopen("file.txt","r");

    fgets(str,100,fp);

    printf("Contents: %s\n",str);

    fclose(fp);

    return 0;
}
